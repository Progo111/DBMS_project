Используемая бд - MySql

Запуск

``` docker compose up -d ```

Выполнение команд 

``` docker compose exec mysql mysql -uroot -prootpass -e "<ЗАПРОС>" ```

Также можно подключиться через разные GUI по localhost и 3306 порту.
